akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

app{
  systemName = "proyectoFinCarreraSpray"
  userServiceName = "demo-service"
  interface = "localhost"
  port = 8080
}

db{
  url = "jdbc:postgresql://localhost/test"
  driver = "org.postgresql.Driver"
  user = "postgres"
  password = "password"
}